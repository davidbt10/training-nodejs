version: '3.8'

services: 
    mysql:
        image: mysql:8.0.24
        ports: 
            - "3306:3306"
        environment: 
            MONGO_INITDB_ROOT_USERNAME: david
            MYSQL_ROOT_PASSWORD: 123456


# docker run -p 3310:3306 -e MYSQL_ROOT_PASSWORD=my_password -e MYSQL_DATABASE=people -d mysql
# docker exec -it exciting_tu mysql -uroot -p

#ALTER USER myuser IDENTIFIED WITH mysql_native_password BY 'mypassword';
#flush privileges;

# create database portal_noticias;
# use portal_noticias;
# create table noticias;
# create table noticias ( id_noticia int not null primary key auto_increment, titulo varchar(100), noticia text, da
# ta_criacao timestamp default current_timestamp);
# insert into noticias(titulo, noticia) values ('titulo da noticia', 'conteudo da noticia');